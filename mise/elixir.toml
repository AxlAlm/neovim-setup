# Elixir language profile tools
# Install with: mise install

[tools]
erlang = "27"
elixir = "1.17"

# LSP server
# Note: elixir-ls must be installed separately
# Visit: https://github.com/elixir-lsp/elixir-ls/releases
# Download and extract to a directory in your PATH, e.g., ~/.local/bin/elixir-ls

# Formatter
# mix format is built into Elixir

# Optional: Additional tools
# hex = "latest"
# rebar3 = "latest"
